# Go Issue Agent

**Agentic AI for fixing Go GitHub issues using Gemini**

This tool takes a GitHub issue URL, clones the repo, explores the code, makes fixes, runs tests (if possible), and generates a PR summary.

## How to Run

### Step 1: Install
```bash
pip install google-genai requests

